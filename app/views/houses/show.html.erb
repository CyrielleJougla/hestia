<h2> Hello </h2>
<p> Name de la house : <%= @house.name %> </p>
<p> Id de la house : <%= @house.id %> </p>
<p> Membres de la house : <%= @house.user.email %> </p>
<h2> SCORE current_user : <%= current_user.profile.score %> </h2>
<h2> Score de la house : </h2>
  <ul>
    <% @house.habitants.each do |habitant| %>
      <li> <%= habitant.user.profile.nickname %> <%= habitant.user.profile.score %> </li>
    <% end %>
  </ul>

<h2> Liste des tâches </h2>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task.name.capitalize, task_path(task) %>
    <%= task.points %> points
  </li>
  <% end %>
</ul>
